# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is not yet scaffolded. It currently contains only a README ("Organize your tour!") describing the project's intent — no source code, build tooling, dependencies, or tests exist yet.

There are no commands to run and no architecture to document. Once the codebase is bootstrapped (framework/language chosen, initial source added), re-run `/init` (or ask Claude to update this file) so it reflects real build/lint/test commands and the actual architecture.

## Basic Instructions

Always grill me with questions about implementation. No question is too small. 
